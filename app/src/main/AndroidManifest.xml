<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.anytrash.manager">

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.anytrash.manager.NotInstalledPackagesActivity"></activity>
        <activity android:name="com.anytrash.manager.PackagesActivity" />
        <activity android:name="com.anytrash.manager.PackageInfoActivity" />
        <activity
            android:name="com.anytrash.manager.ShellActivity"
            android:label="@string/shell" />
        <activity
            android:name="com.anytrash.manager.AboutActivity"
            android:label="@string/about"
            android:theme="@style/Theme.AppCompat.Dialog" />
        <activity android:name="com.anytrash.manager.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>